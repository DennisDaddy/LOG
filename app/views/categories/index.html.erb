<h1>All Categories</h1>
<%= link_to "Add Category", new_category_path %> 
<ul>
<% @categories.each do |category| %>

<li><%= link_to  category.name, category %> |<%= link_to "Delete", category, method: :delete,
data: { confirm: "You sure?" } %> </li>

<% end %> 
</ul>