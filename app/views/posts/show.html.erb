<h1><%= @post.title %> </h1>
   <small><%= @post.created_at.strftime("%b %d %Y") %></small>

<p><%= @post.body %> </p>

<p> <b>Category:  </b><%= link_to @post.category.name, category_path(:id => @post.category.id) %></p>


<%= link_to "Edit Post", edit_post_path %> | 
<%= link_to "Delete", @post, method: :delete,
data: { confirm: "You sure?" } %> <br><br>

<%= link_to "Go Back", posts_path %>

<hr>
<h3>Add Comment</h3>

<%= form_for(@comment) do |f| %>
       <p>
		<%= f.label :name %>
   	    <%= f.text_field :name %>
   	    </pd
   	    <p>
		<%= f.label :email %>
   	    <%= f.text_field :email %>
   	    </p>

   	    <p>
        <%= f.label :body %>
		<%= f.text_area :body %>
		</p>
		
		<%= f.submit "Post Comment" %>

<% end %>